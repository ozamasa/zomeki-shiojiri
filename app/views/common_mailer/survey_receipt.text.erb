「<%= @form_answer.form.title %>」に問い合わせがありました。

<%- @form_answer.answers.each do |answer| -%>
<%= "#{answer.question.title}：#{answer.content}" %>
<%- end -%>
<%= "#{@form_answer.class.human_attribute_name :created_at}：#{l @form_answer.created_at}" %>

<%= "#{URI.parse(@form_answer.answered_url).scheme}://#{URI.parse(@form_answer.answered_url).host}#{survey_form_form_answers_path(content: @form_answer.form.content, form_id: @form_answer.form.id)}/#{@form_answer.id}" %>
